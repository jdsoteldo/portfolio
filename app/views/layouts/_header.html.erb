<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <h2><a href="/" class="navbar-brand">JD SOTELDO</a></h2>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse display-right" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <% if current_user && current_user.role == "admin" %>
        <%= link_to 'New Post', new_user_post_path(current_user.id), class:"nav-link active" %>
        <%= link_to 'New Task', new_task_path, class: "nav-link active" %>
      <% end %>
      <% if current_page?("/") %>
        <a href="#technologies" class="nav-link active">Technologies</a>
        <a href="#work" class="nav-link active">Work</a>
        <a href="#goals" class="nav-link active">Goals</a>
        <a href="#references" class="nav-link active">References</a>
        <a href="#contact" class="nav-link active">Contact Me</a>
        <%= link_to 'Check the Blog', posts_path, id:"blog-header-link", class: "nav-link active" %>
      <% else %>
        <%= link_to 'Home', root_path, class: "nav-link active" %>
      <% end %>

      <% if current_user %>
        <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class:"nav-link active" %>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path, class:"nav-link active" %>
      <% end %>

    </div>
  </div>
</nav>
